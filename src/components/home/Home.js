import React from 'react';

import News from '../news/News';
import styles from './home.module.css';

const Home = (props) => {
    return (
      <div>
        <News
        />
      </div>
    );
}

export default Home;
